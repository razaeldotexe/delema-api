[phases.setup]
nixPkgs = ["nodejs", "pnpm", "google-chrome", "chromium", "playwright-driver"]

[phases.build]
dependsOn = ["setup"]
cmds = ["pnpm install", "pnpm run build"]

[start]
cmd = "node dist/main.js"
